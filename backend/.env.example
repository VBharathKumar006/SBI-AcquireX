PORT=4000
JWT_SECRET=replace-with-a-development-secret
MONGODB_URI=mongodb://localhost:27017/sbi-acquirex
GEMINI_API_KEY=
CHROMA_URL=http://localhost:8000
CONVEX_URL=

# Security
CORS_ORIGINS=http://localhost:3000
RATE_LIMIT_MAX=200

# Push Notifications (Web Push) — generate keys via: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=your-vapid-public-key
# VAPID_PRIVATE_KEY=your-vapid-private-key
